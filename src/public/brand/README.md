# Runtime Brand Assets

This directory contains runtime copies of the approved Environment Fund SVG assets.

Expected structure:

```text
src/public/brand/
├── logos/
├── patterns/
└── graphic-elements/
```

Upload only official approved SVG files. Do not add licensed font files to the repository.
